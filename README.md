
  # THEANDY444

  This is a code bundle for THEANDY444. The original project is available at Figma

  ## Running the code

  Run `npm i` to install the dependencies.

  Run `npm run dev` to start the development server.
  
